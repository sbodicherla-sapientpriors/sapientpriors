# The build pipeline is source, not site. Without this it was served verbatim
# at /build/*.py - readable by anyone who guessed the path.
build/
*.md
# Same reasoning: deploy/ is operator tooling run by hand, not something the site serves.
deploy/
